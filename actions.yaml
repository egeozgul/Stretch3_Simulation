# Stretch 3 Robot Action Definitions
# This file defines macro actions (complex behaviors) composed of micro actions (primitive commands)
#
# Structure:
#   - micro_actions: Basic robot commands (go to anchor, reset arm, etc.)
#   - macro_actions: Complex behaviors built from micro actions
#
# Usage:
#   Actions can reference other actions, allowing composition and reuse
#   Parameters can be passed to actions for flexibility

# ============================================================================
# MICRO ACTIONS (Primitive Commands)
# ============================================================================
# These are the basic building blocks that the robot can execute directly

micro_actions:
  # Navigation actions
  - name: "go_to_anchor"
    type: "navigation"
    description: "Navigate to a specific anchor point without turning (A, B, C, D, E, F, ORIGIN) (Usage: go_to_anchor anchor=A speed=0.5 position_tolerance=0.15)"
    parameters:
      anchor: "string"  # Anchor identifier: A, B, C, D, E, F, or ORIGIN
      speed: "float"  # Optional: Speed 0-1 (default: 0.5). 0=min, 0.5=middle, 1=max
      position_tolerance: "float"  # Optional: Position tolerance in meters (default: 0.15). Action completes when within this distance of target
    
  - name: "turn_towards"
    type: "navigation"
    description: "Turn robot towards a specific anchor or absolute angle without moving (Usage: turn_towards anchor=ORIGIN speed=0.5 delta_angle=5.0 OR turn_towards degrees=90.0 speed=0.5 delta_angle=5.0)"
    parameters:
      anchor: "string"  # Anchor identifier: A, B, C, D, E, F, or ORIGIN (mutually exclusive with degrees)
      degrees: "float"  # Optional: Absolute target angle in degrees (0-360). Mutually exclusive with anchor. If specified, robot turns to this absolute heading
      speed: "float"  # Optional: Speed 0-1 (default: 0.5). 0=min, 0.5=middle, 1=max
      delta_angle: "float"  # Optional: Delta angle in degrees (default: 5.0). Action completes when within this angle of target heading
    
  - name: "go_to_position"
    type: "navigation"
    description: "Navigate to a specific (x, y) position (Usage: go_to_position x=0.5 y=0.5 speed=0.5)"
    parameters:
      x: "float"  # X position 0-1 (0=min, 0.5=middle, 1=max)
      y: "float"  # Y position 0-1 (0=min, 0.5=middle, 1=max)
      direction: "float"  # Optional: target orientation 0-1 (0=0°, 0.5=180°, 1=360°)
      speed: "float"  # Optional: Speed 0-1 (default: 0.5). 0=min, 0.5=middle, 1=max
#      
  - name: "align_with_target"
    type: "navigation"
    description: "Align robot to face a target object (Usage: align_with_target target=tomato1 speed=0.5)"
    parameters:
      target: "string"  # Target object name (e.g., "tomato1", "lettuce1")
      speed: "float"    # Optional: Speed 0-1 (default: 0.5)
      delta_angle: "float"  # Optional: Angle tolerance in degrees (default: 5.0)
  
  - name: "compute_ik"
    type: "utility"
    description: "Compute IK for target position and store results (Usage: compute_ik target=tomato3 speed=0.5)"
    parameters:
      target: "string"  # Target object name (e.g., "tomato1", "tomato3", "lettuce1")
      speed: "float"    # Optional: Speed 0-1 (default: 0.5)

#
  # Arm control actions
  - name: "reset_arm"
    type: "arm_control"
    description: "Reset arm to default position (lift up, arm retracted, gripper open, wrist centered) (Usage: reset_arm speed=0.5)"
    parameters:
      speed: "float"  # Optional: Speed 0-1 (default: 0.5). Controls how fast all actuators move together. 0=min, 0.5=middle, 1=max
    
  - name: "elevate_arm"
    type: "arm_control"
    description: "Move lift to a specific height (Usage: elevate_arm height=0.5 speed=0.5)"
    parameters:
      height: "float"  # Height 0-1 (0=min, 0.5=middle, 1=max)
      speed: "float"  # Optional: Speed 0-1 (default: 0.5). 0=min, 0.5=middle, 1=max
    
  - name: "extend_arm"
    type: "arm_control"
    description: "Extend or retract the arm to a specific length (Usage: extend_arm length=0.5 speed=0.5)"
    parameters:
      length: "float"  # Extension length 0-1 (0=min/retracted, 0.5=middle, 1=max/extended)
      speed: "float"  # Optional: Speed 0-1 (default: 0.5). 0=min, 0.5=middle, 1=max
    
  - name: "rotate_wrist"
    type: "arm_control"
    description: "Rotate wrist yaw to a specific angle (Usage: rotate_wrist angle=0.5 speed=0.5)"
    parameters:
      angle: "float"  # Angle 0-1 (0=min, 0.5=middle, 1=max)
      speed: "float"  # Optional: Speed 0-1 (default: 0.5). 0=min, 0.5=middle, 1=max
    
  - name: "pitch_wrist"
    type: "arm_control"
    description: "Pitch wrist (tilt up/down) to a specific angle (Usage: pitch_wrist angle=0.5 speed=0.5)"
    parameters:
      angle: "float"  # Angle 0-1 (0=min/down, 0.5=middle, 1=max/up)
      speed: "float"  # Optional: Speed 0-1 (default: 0.5). 0=min, 0.5=middle, 1=max
    
  - name: "roll_wrist"
    type: "arm_control"
    description: "Roll wrist (rotate around gripper axis) to a specific angle (Usage: roll_wrist angle=0.5 speed=0.5)"
    parameters:
      angle: "float"  # Angle 0-1 (0=min, 0.5=middle, 1=max)
      speed: "float"  # Optional: Speed 0-1 (default: 0.5). 0=min, 0.5=middle, 1=max
    
  - name: "set_gripper"
    type: "arm_control"
    description: "Set gripper to a specific opening width (Usage: set_gripper width=0.5 speed=0.5)"
    parameters:
      width: "float"  # Width 0-1 (0=closed, 0.5=middle, 1=open)
      speed: "float"  # Optional: Speed 0-1 (default: 0.5). 0=min, 0.5=middle, 1=max
    
  # Wait/delay actions
  - name: "wait"
    type: "utility"
    description: "Wait for a specified duration (Usage: wait duration=2.0)"
    parameters:
      duration: "float"  # Duration in seconds
    
  - name: "wait_for_arm"
    type: "utility"
    description: "Wait until arm reaches target position (Usage: wait_for_arm timeout=5.0)"
    parameters:
      timeout: "float"  # Maximum wait time in seconds (optional)

# ============================================================================
# MACRO ACTIONS (Complex Behaviors)
# ============================================================================
# These are composed of sequences of micro actions and/or other macro actions

macro_actions:
  # Basic macro: Do nothing (stay in place)
  - name: "stay"
    description: "Stay in current position, do nothing"
    sequence:
      - action: "wait"
        parameters:
          duration: 0.1  # Minimal wait to keep action valid
    
  # Object retrieval macros
  - name: "get_tomato1"
    description: "Navigate to tomato location and pick it up using IK"
    sequence:
      - action: "reset_arm"
        parameters:
          speed: 0.5
      
      - action: "turn_towards"
        parameters:
          anchor: "ORIGIN"
          speed: 0.5
      
      - action: "wait"
        parameters:
          duration: 0.5
      
      - action: "go_to_anchor"
        parameters:
          anchor: "D"
          speed: 0.9
      
      - action: "wait"
        parameters:
          duration: 1.0
      
      # Align with target
      - action: "align_with_target"
        parameters:
          target: "tomato1"
          speed: 0.5
          delta_angle: 1.0
      
      - action: "wait"
        parameters:
          duration: 1.0
      
      # Compute IK
      - action: "compute_ik"
        parameters:
          target: "tomato1"
          speed: 0.5
      
      - action: "wait"
        parameters:
          duration: 1.0
      
      # Use IK values - REMOVE manual values, ADD use_ik: 1
      - action: "rotate_wrist"
        parameters:
          use_ik: 1      # ← USE IK VALUE
          speed: 0.02
      
      - action: "wait"
        parameters:
          duration: 2.0
      
      - action: "extend_arm"
        parameters:
          use_ik: 1      # ← USE IK VALUE
          speed: 0.01
      
      - action: "wait"
        parameters:
          duration: 3.0
      
      - action: "elevate_arm"
        parameters:
          use_ik: 1      # ← USE IK VALUE
          speed: 0.01
      
      - action: "wait"
        parameters:
          duration: 4.0
      
      # Open gripper
      - action: "set_gripper"
        parameters:
          width: 0.9
          speed: 0.01
      
      - action: "wait"
        parameters:
          duration: 2.0
      
      # Close gripper to grasp
      - action: "set_gripper"
        parameters:
          width: 0.0
          speed: 0.01
      
      - action: "wait"
        parameters:
          duration: 2.0
      
      # Lift up (manual height, not IK)
      - action: "elevate_arm"
        parameters:
          height: 0.8    # Manual 0-1 value (not IK)
          speed: 0.01
      
      - action: "wait"
        parameters:
          duration: 1.0
  - name: "get_tomato2"
    description: "Navigate to tomato location and pick it up using IK"
    sequence:
      - action: "reset_arm"
        parameters:
          speed: 0.5
      
      - action: "turn_towards"
        parameters:
          anchor: "ORIGIN"
          speed: 0.5
      
      - action: "wait"
        parameters:
          duration: 0.5
      
      - action: "go_to_anchor"
        parameters:
          anchor: "D"
          speed: 0.9
      
      - action: "wait"
        parameters:
          duration: 1.0
      
      # Align with target
      - action: "align_with_target"
        parameters:
          target: "tomato2"
          speed: 0.5
          delta_angle: 1.0
      
      - action: "wait"
        parameters:
          duration: 1.0
      
      # Compute IK
      - action: "compute_ik"
        parameters:
          target: "tomato2"
          speed: 0.5
      
      - action: "wait"
        parameters:
          duration: 1.0
      
      # Use IK values - REMOVE manual values, ADD use_ik: 1
      - action: "rotate_wrist"
        parameters:
          use_ik: 1      # ← USE IK VALUE
          speed: 0.02
      
      - action: "wait"
        parameters:
          duration: 2.0
      
      - action: "extend_arm"
        parameters:
          use_ik: 1      # ← USE IK VALUE
          speed: 0.01
      
      - action: "wait"
        parameters:
          duration: 3.0
      
      - action: "elevate_arm"
        parameters:
          use_ik: 1      # ← USE IK VALUE
          speed: 0.01
      
      - action: "wait"
        parameters:
          duration: 4.0
      
      # Open gripper
      - action: "set_gripper"
        parameters:
          width: 0.9
          speed: 0.01
      
      - action: "wait"
        parameters:
          duration: 2.0
      
      # Close gripper to grasp
      - action: "set_gripper"
        parameters:
          width: 0.0
          speed: 0.01
      
      - action: "wait"
        parameters:
          duration: 2.0
      
      # Lift up (manual height, not IK)
      - action: "elevate_arm"
        parameters:
          height: 0.8    # Manual 0-1 value (not IK)
          speed: 0.01
      
      - action: "wait"
        parameters:
          duration: 1.0
  - name: "get_tomato3"
    description: "Navigate to tomato location and pick it up using IK"
    sequence:
      - action: "reset_arm"
        parameters:
          speed: 0.5
      
      - action: "turn_towards"
        parameters:
          anchor: "ORIGIN"
          speed: 0.5
      
      - action: "wait"
        parameters:
          duration: 0.5
      
      - action: "go_to_anchor"
        parameters:
          anchor: "D"
          speed: 0.9
      
      - action: "wait"
        parameters:
          duration: 1.0
      
      # Align with target
      - action: "align_with_target"
        parameters:
          target: "tomato3"
          speed: 0.5
          delta_angle: 1.0
      
      - action: "wait"
        parameters:
          duration: 1.0
      
      # Compute IK
      - action: "compute_ik"
        parameters:
          target: "tomato3"
          speed: 0.5
      
      - action: "wait"
        parameters:
          duration: 1.0
      
      # Use IK values - REMOVE manual values, ADD use_ik: 1
      - action: "rotate_wrist"
        parameters:
          use_ik: 1      # ← USE IK VALUE
          speed: 0.02
      
      - action: "wait"
        parameters:
          duration: 2.0
      
      - action: "extend_arm"
        parameters:
          use_ik: 1      # ← USE IK VALUE
          speed: 0.01
      
      - action: "wait"
        parameters:
          duration: 3.0
      
      - action: "elevate_arm"
        parameters:
          use_ik: 1      # ← USE IK VALUE
          speed: 0.01
      
      - action: "wait"
        parameters:
          duration: 4.0
      
      # Open gripper
      - action: "set_gripper"
        parameters:
          width: 0.9
          speed: 0.01
      
      - action: "wait"
        parameters:
          duration: 2.0
      
      # Close gripper to grasp
      - action: "set_gripper"
        parameters:
          width: 0.0
          speed: 0.01
      
      - action: "wait"
        parameters:
          duration: 2.0
      
      # Lift up (manual height, not IK)
      - action: "elevate_arm"
        parameters:
          height: 0.8    # Manual 0-1 value (not IK)
          speed: 0.01
      
      - action: "wait"
        parameters:
          duration: 1.0
  
  - name: "get_lettuce1"
    description: "Navigate to tomato location and pick it up using IK"
    sequence:
      - action: "reset_arm"
        parameters:
          speed: 0.5
      
      - action: "turn_towards"
        parameters:
          anchor: "ORIGIN"
          speed: 0.5
      
      - action: "wait"
        parameters:
          duration: 0.5
      
      - action: "go_to_anchor"
        parameters:
          anchor: "A"
          speed: 0.9
      
      - action: "wait"
        parameters:
          duration: 1.0
      
      # Align with target
      - action: "align_with_target"
        parameters:
          target: "lettuce1"
          speed: 0.5
          delta_angle: 1.0
      
      - action: "wait"
        parameters:
          duration: 1.0
      
      # Compute IK
      - action: "compute_ik"
        parameters:
          target: "lettuce1"
          speed: 0.5
      
      - action: "wait"
        parameters:
          duration: 1.0
      
      # Use IK values - REMOVE manual values, ADD use_ik: 1
      - action: "rotate_wrist"
        parameters:
          use_ik: 1      # ← USE IK VALUE
          speed: 0.02
      
      - action: "wait"
        parameters:
          duration: 2.0
      
      - action: "extend_arm"
        parameters:
          use_ik: 1      # ← USE IK VALUE
          speed: 0.01
      
      - action: "wait"
        parameters:
          duration: 3.0
      
      - action: "elevate_arm"
        parameters:
          use_ik: 1      # ← USE IK VALUE
          speed: 0.01
      
      - action: "wait"
        parameters:
          duration: 4.0
      
      # Open gripper
      - action: "set_gripper"
        parameters:
          width: 0.9
          speed: 0.01
      
      - action: "wait"
        parameters:
          duration: 2.0
      
      # Close gripper to grasp
      - action: "set_gripper"
        parameters:
          width: 0.0
          speed: 0.01
      
      - action: "wait"
        parameters:
          duration: 2.0
      
      # Lift up (manual height, not IK)
      - action: "elevate_arm"
        parameters:
          height: 0.8    # Manual 0-1 value (not IK)
          speed: 0.01
      
      - action: "wait"
        parameters:
          duration: 1.0
  - name: "get_lettuce2"
    description: "Navigate to tomato location and pick it up using IK"
    sequence:
      - action: "reset_arm"
        parameters:
          speed: 0.5
      
      - action: "turn_towards"
        parameters:
          anchor: "ORIGIN"
          speed: 0.5
      
      - action: "wait"
        parameters:
          duration: 0.5
      
      - action: "go_to_anchor"
        parameters:
          anchor: "A"
          speed: 0.9
      
      - action: "wait"
        parameters:
          duration: 1.0
      
      # Align with target
      - action: "align_with_target"
        parameters:
          target: "lettuce2"
          speed: 0.5
          delta_angle: 1.0
      
      - action: "wait"
        parameters:
          duration: 1.0
      
      # Compute IK
      - action: "compute_ik"
        parameters:
          target: "lettuce2"
          speed: 0.5
      
      - action: "wait"
        parameters:
          duration: 1.0
      
      # Use IK values - REMOVE manual values, ADD use_ik: 1
      - action: "rotate_wrist"
        parameters:
          use_ik: 1      # ← USE IK VALUE
          speed: 0.02
      
      - action: "wait"
        parameters:
          duration: 2.0
      
      - action: "extend_arm"
        parameters:
          use_ik: 1      # ← USE IK VALUE
          speed: 0.01
      
      - action: "wait"
        parameters:
          duration: 3.0
      
      - action: "elevate_arm"
        parameters:
          use_ik: 1      # ← USE IK VALUE
          speed: 0.01
      
      - action: "wait"
        parameters:
          duration: 4.0
      
      # Open gripper
      - action: "set_gripper"
        parameters:
          width: 0.9
          speed: 0.01
      
      - action: "wait"
        parameters:
          duration: 2.0
      
      # Close gripper to grasp
      - action: "set_gripper"
        parameters:
          width: 0.0
          speed: 0.01
      
      - action: "wait"
        parameters:
          duration: 2.0
      
      # Lift up (manual height, not IK)
      - action: "elevate_arm"
        parameters:
          height: 0.8    # Manual 0-1 value (not IK)
          speed: 0.01
      
      - action: "wait"
        parameters:
          duration: 1.0
  - name: "get_lettuce3"
    description: "Navigate to tomato location and pick it up using IK"
    sequence:
      - action: "reset_arm"
        parameters:
          speed: 0.5
      
      - action: "turn_towards"
        parameters:
          anchor: "ORIGIN"
          speed: 0.5
      
      - action: "wait"
        parameters:
          duration: 0.5
      
      - action: "go_to_anchor"
        parameters:
          anchor: "A"
          speed: 0.9
      
      - action: "wait"
        parameters:
          duration: 1.0
      
      # Align with target
      - action: "align_with_target"
        parameters:
          target: "lettuce3"
          speed: 0.5
          delta_angle: 1.0
      
      - action: "wait"
        parameters:
          duration: 1.0
      
      # Compute IK
      - action: "compute_ik"
        parameters:
          target: "lettuce3"
          speed: 0.5
      
      - action: "wait"
        parameters:
          duration: 1.0
      
      # Use IK values - REMOVE manual values, ADD use_ik: 1
      - action: "rotate_wrist"
        parameters:
          use_ik: 1      # ← USE IK VALUE
          speed: 0.02
      
      - action: "wait"
        parameters:
          duration: 2.0
      
      - action: "extend_arm"
        parameters:
          use_ik: 1      # ← USE IK VALUE
          speed: 0.01
      
      - action: "wait"
        parameters:
          duration: 3.0
      
      - action: "elevate_arm"
        parameters:
          use_ik: 1      # ← USE IK VALUE
          speed: 0.01
      
      - action: "wait"
        parameters:
          duration: 4.0
      
      # Open gripper
      - action: "set_gripper"
        parameters:
          width: 0.9
          speed: 0.01
      
      - action: "wait"
        parameters:
          duration: 2.0
      
      # Close gripper to grasp
      - action: "set_gripper"
        parameters:
          width: 0.0
          speed: 0.01
      
      - action: "wait"
        parameters:
          duration: 2.0
      
      # Lift up (manual height, not IK)
      - action: "elevate_arm"
        parameters:
          height: 0.8    # Manual 0-1 value (not IK)
          speed: 0.01
      
      - action: "wait"
        parameters:
          duration: 1.0

  - name: "get_onion1"
    description: "Navigate to tomato location and pick it up using IK"
    sequence:
      - action: "reset_arm"
        parameters:
          speed: 0.5
      
      - action: "turn_towards"
        parameters:
          anchor: "ORIGIN"
          speed: 0.5
      
      - action: "wait"
        parameters:
          duration: 0.5
      
      - action: "go_to_anchor"
        parameters:
          anchor: "C"
          speed: 0.9
      
      - action: "wait"
        parameters:
          duration: 1.0
      
      # Align with target
      - action: "align_with_target"
        parameters:
          target: "onion1"
          speed: 0.5
          delta_angle: 1.0
      
      - action: "wait"
        parameters:
          duration: 1.0
      
      # Compute IK
      - action: "compute_ik"
        parameters:
          target: "onion1"
          speed: 0.5
      
      - action: "wait"
        parameters:
          duration: 1.0
      
      # Use IK values - REMOVE manual values, ADD use_ik: 1
      - action: "rotate_wrist"
        parameters:
          use_ik: 1      # ← USE IK VALUE
          speed: 0.02
      
      - action: "wait"
        parameters:
          duration: 2.0
      
      - action: "extend_arm"
        parameters:
          use_ik: 1      # ← USE IK VALUE
          speed: 0.01
      
      - action: "wait"
        parameters:
          duration: 3.0
      
      - action: "elevate_arm"
        parameters:
          use_ik: 1      # ← USE IK VALUE
          speed: 0.01
      
      - action: "wait"
        parameters:
          duration: 4.0
      
      # Open gripper
      - action: "set_gripper"
        parameters:
          width: 0.9
          speed: 0.01
      
      - action: "wait"
        parameters:
          duration: 2.0
      
      # Close gripper to grasp
      - action: "set_gripper"
        parameters:
          width: 0.0
          speed: 0.01
      
      - action: "wait"
        parameters:
          duration: 2.0
      
      # Lift up (manual height, not IK)
      - action: "elevate_arm"
        parameters:
          height: 0.8    # Manual 0-1 value (not IK)
          speed: 0.01
      
      - action: "wait"
        parameters:
          duration: 1.0
  - name: "get_onion2"
    description: "Navigate to tomato location and pick it up using IK"
    sequence:
      - action: "reset_arm"
        parameters:
          speed: 0.5
      
      - action: "turn_towards"
        parameters:
          anchor: "ORIGIN"
          speed: 0.5
      
      - action: "wait"
        parameters:
          duration: 0.5
      
      - action: "go_to_anchor"
        parameters:
          anchor: "C"
          speed: 0.9
      
      - action: "wait"
        parameters:
          duration: 1.0
      
      # Align with target
      - action: "align_with_target"
        parameters:
          target: "onion2"
          speed: 0.5
          delta_angle: 1.0
      
      - action: "wait"
        parameters:
          duration: 1.0
      
      # Compute IK
      - action: "compute_ik"
        parameters:
          target: "onion2"
          speed: 0.5
      
      - action: "wait"
        parameters:
          duration: 1.0
      
      # Use IK values - REMOVE manual values, ADD use_ik: 1
      - action: "rotate_wrist"
        parameters:
          use_ik: 1      # ← USE IK VALUE
          speed: 0.02
      
      - action: "wait"
        parameters:
          duration: 2.0
      
      - action: "extend_arm"
        parameters:
          use_ik: 1      # ← USE IK VALUE
          speed: 0.01
      
      - action: "wait"
        parameters:
          duration: 3.0
      
      - action: "elevate_arm"
        parameters:
          use_ik: 1      # ← USE IK VALUE
          speed: 0.01
      
      - action: "wait"
        parameters:
          duration: 4.0
      
      # Open gripper
      - action: "set_gripper"
        parameters:
          width: 0.9
          speed: 0.01
      
      - action: "wait"
        parameters:
          duration: 2.0
      
      # Close gripper to grasp
      - action: "set_gripper"
        parameters:
          width: 0.0
          speed: 0.01
      
      - action: "wait"
        parameters:
          duration: 2.0
      
      # Lift up (manual height, not IK)
      - action: "elevate_arm"
        parameters:
          height: 0.8    # Manual 0-1 value (not IK)
          speed: 0.01
      
      - action: "wait"
        parameters:
          duration: 1.0
  - name: "get_onion3"
    description: "Navigate to tomato location and pick it up using IK"
    sequence:
      - action: "reset_arm"
        parameters:
          speed: 0.5
      
      - action: "turn_towards"
        parameters:
          anchor: "ORIGIN"
          speed: 0.5
      
      - action: "wait"
        parameters:
          duration: 0.5
      
      - action: "go_to_anchor"
        parameters:
          anchor: "C"
          speed: 0.9
      
      - action: "wait"
        parameters:
          duration: 1.0
      
      # Align with target
      - action: "align_with_target"
        parameters:
          target: "onion3"
          speed: 0.5
          delta_angle: 1.0
      
      - action: "wait"
        parameters:
          duration: 1.0
      
      # Compute IK
      - action: "compute_ik"
        parameters:
          target: "onion3"
          speed: 0.5
      
      - action: "wait"
        parameters:
          duration: 1.0
      
      # Use IK values - REMOVE manual values, ADD use_ik: 1
      - action: "rotate_wrist"
        parameters:
          use_ik: 1      # ← USE IK VALUE
          speed: 0.02
      
      - action: "wait"
        parameters:
          duration: 2.0
      
      - action: "extend_arm"
        parameters:
          use_ik: 1      # ← USE IK VALUE
          speed: 0.01
      
      - action: "wait"
        parameters:
          duration: 3.0
      
      - action: "elevate_arm"
        parameters:
          use_ik: 1      # ← USE IK VALUE
          speed: 0.01
      
      - action: "wait"
        parameters:
          duration: 4.0
      
      # Open gripper
      - action: "set_gripper"
        parameters:
          width: 0.9
          speed: 0.01
      
      - action: "wait"
        parameters:
          duration: 2.0
      
      # Close gripper to grasp
      - action: "set_gripper"
        parameters:
          width: 0.0
          speed: 0.01
      
      - action: "wait"
        parameters:
          duration: 2.0
      
      # Lift up (manual height, not IK)
      - action: "elevate_arm"
        parameters:
          height: 0.8    # Manual 0-1 value (not IK)
          speed: 0.01
      
      - action: "wait"
        parameters:
          duration: 1.0

  - name: "cut_fruit"
    description: "Cut fruit already in gripper at knife location (table 1, middle)"
    sequence:
      # Step 1: Navigate to knife location
      - action: "turn_towards"
        parameters:
          anchor: "ORIGIN"
          speed: 0.5
      
      - action: "wait"
        parameters:
          duration: 0.5
      
      - action: "go_to_anchor"
        parameters:
          anchor: "B"
          speed: 0.9
      
      - action: "wait"
        parameters:
          duration: 1.0
      
      # Step 2: Align with knife target
      - action: "align_with_target"
        parameters:
          target: "cut"  # or "knife" depending on your target naming
          speed: 0.5
          delta_angle: 1.0
      
      - action: "wait"
        parameters:
          duration: 1.0
      
      # Step 3: Compute IK for cutting position
      - action: "compute_ik"
        parameters:
          target: "cut"
          speed: 0.5
      
      - action: "wait"
        parameters:
          duration: 1.0
      
      # Step 4: Move to cutting position using IK
      - action: "rotate_wrist"
        parameters:
          use_ik: 1
          speed: 0.02
      
      - action: "wait"
        parameters:
          duration: 2.0
      
      - action: "extend_arm"
        parameters:
          use_ik: 1
          speed: 0.01
      
      - action: "wait"
        parameters:
          duration: 3.0
      
      - action: "elevate_arm"
        parameters:
          use_ik: 1
          speed: 0.01
      
      - action: "wait"
        parameters:
          duration: 4.0
      
      - action: "set_gripper"
        parameters:
          width: 0.9
          speed: 0.01
      
      - action: "wait"
        parameters:
          duration: 2.0
      
      # Step 6: Lift up to clear the cutting area
      - action: "elevate_arm"
        parameters:
          height: 0.8
          speed: 0.01
      
      - action: "wait"
        parameters:
          duration: 2.0
      
      # Step 7: Close gripper (ready for next action)
      - action: "set_gripper"
        parameters:
          width: 0.0
          speed: 0.01

      - action: "wait"
        parameters:
          duration: 1.0

      - action: "reset_arm"  
        parameters:
          speed: 0.5

      - action: "wait"
        parameters:
          duration: 1.0

  - name: "plate_fruit"
    description: "Cut fruit already in gripper at knife location (table 1, middle)"
    sequence:
      # Step 1: Navigate to knife location
      - action: "turn_towards"
        parameters:
          anchor: "ORIGIN"
          speed: 0.5
      
      - action: "wait"
        parameters:
          duration: 0.5
      
      - action: "go_to_anchor"
        parameters:
          anchor: "F"
          speed: 0.9
      
      - action: "wait"
        parameters:
          duration: 1.0
      
      # Step 2: Align with knife target
      - action: "align_with_target"
        parameters:
          target: "plate"  # or "knife" depending on your target naming
          speed: 0.5
          delta_angle: 1.0
      
      - action: "wait"
        parameters:
          duration: 1.0
      
      # Step 3: Compute IK for cutting position
      - action: "compute_ik"
        parameters:
          target: "plate"
          speed: 0.5
      
      - action: "wait"
        parameters:
          duration: 1.0
      
      # Step 4: Move to cutting position using IK
      - action: "rotate_wrist"
        parameters:
          use_ik: 1
          speed: 0.02
      
      - action: "wait"
        parameters:
          duration: 2.0
      
      - action: "extend_arm"
        parameters:
          use_ik: 1
          speed: 0.01
      
      - action: "wait"
        parameters:
          duration: 3.0
      
      - action: "elevate_arm"
        parameters:
          use_ik: 1
          speed: 0.01
      
      - action: "wait"
        parameters:
          duration: 4.0
      
      # Step 5: Open gripper to release fruit onto knife
      - action: "set_gripper"
        parameters:
          width: 0.9
          speed: 0.01
      
      - action: "wait"
        parameters:
          duration: 2.0
      
      # Step 6: Lift up to clear the cutting area
      - action: "elevate_arm"
        parameters:
          height: 0.8
          speed: 0.01
      
      - action: "wait"
        parameters:
          duration: 4.0
      
      - action: "set_gripper"
        parameters:
          width: 0.0
          speed: 0.01

      - action: "wait"
        parameters:
          duration: 1.0

      - action: "reset_arm"  # ← Add this
        parameters:
          speed: 0.5

      - action: "wait"
        parameters:
          duration: 1.0
          
  - name: "get_plate"
    description: "Navigate to plate location and pick it up"
    sequence:
      - action: "reset_arm"
        parameters:
          speed: 0.01
      - action: "turn_towards"
        parameters:
          anchor: "ORIGIN"
          speed: 0.5
      - action: "wait"
        parameters:
          duration: 10
      - action: "go_to_anchor"
        parameters:
          anchor: "E"
          speed: 0.9
      # TODO: Define sequence of actions
      # Plates are on table 3
      - action: "wait"
        parameters:
          duration: 0.1
    
  - name: "go_to_knife"
    description: "Navigate to knife location"
    sequence:
      - action: "reset_arm"
        parameters:
          speed: 0.01
      - action: "turn_towards"
        parameters:
          anchor: "ORIGIN"
          speed: 0.5
      - action: "wait"
        parameters:
          duration: 10
      - action: "go_to_anchor"
        parameters:
          anchor: "B"
          speed: 0.9
      # TODO: Define sequence of actions
      # Knife is in the middle of table 1
      - action: "wait"
        parameters:
          duration: 0.1
    
  # Delivery macro
  - name: "deliver"
    description: "Navigate to delivery station and place object"
    sequence:
      - action: "turn_towards"
        parameters:
          anchor: "ORIGIN"
          speed: 0.5
      - action: "wait"
        parameters:
          duration: 10
      - action: "go_to_anchor"
        parameters:
          anchor: "F"
          speed: 0.9
      # TODO: Define sequence of actions
      # Delivery station is on table 3
      # - action: "go_to_anchor"
      #   parameters:
      #     anchor: "E"  # Example anchor near delivery station
      # - action: "elevate_arm"
      #   parameters:
      #     height: 0.5
      # - action: "open_gripper"
      - action: "wait"
        parameters:
          duration: 0.1

# ============================================================================
# ACTION PARAMETERS AND CONSTANTS
# ============================================================================
# Define common parameters and constants that can be referenced in actions

constants:
  # Anchor mappings (to be populated based on actual anchor positions)
  anchors:
    A: "Table 1, left side"
    B: "Table 1, right side"
    C: "Table 2, left side"
    D: "Table 2, right side"
    E: "Table 3 area"
    F: "Table 3 area"
  
  # Object locations (approximate positions - to be refined)
  object_positions:
    tomato:
      table: 2
      side: "right"
      approximate_y: 4.5
    lettuce:
      table: 1
      side: "left"
      approximate_y: 0.5
    onion:
      table: 2
      side: "left"
      approximate_y: 4.5
    plate:
      table: 3
      approximate_x: 2.4
      approximate_y: 2.5
    knife:
      table: 1
      approximate_x: 0.0
      approximate_y: 0.0
  
  # Arm positions
  arm_positions:
    default_lift: 0.6
    default_arm_extend: 0.0
    default_wrist_yaw: 1.125
    default_gripper: 0.04
    pickup_height: 0.4
    delivery_height: 0.5

# ============================================================================
# NOTES FOR IMPLEMENTATION
# ============================================================================
# 
# When implementing:
# 1. Each micro action should map to a ROS2 command or function call
# 2. Macro actions should execute their sequence sequentially
# 3. Add error handling and timeout mechanisms
# 4. Consider adding conditional logic (if/then) for complex behaviors
# 5. May want to add action validation before execution
# 6. Consider adding action status/feedback mechanisms
# 7. Could add action composition (macro actions calling other macro actions)
#
# Future enhancements:
# - Add parallel action execution
# - Add action preconditions and postconditions
# - Add action failure recovery strategies
# - Add action monitoring and logging
# - Add action parameter validation

